[project]
name = "vitok"
version = "0.1.0"
description = "SuperBPE and NFD tokenizers for small Vietnamese language models"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
